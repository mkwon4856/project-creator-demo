# Project Creator — rebuild Task 22
## 캠페인 생성 2단계 — 하단 예산영역 제거 + 예상 참여 인원 표시

목표: 캠페인 생성 위저드 2단계(미션 구성)에서 "사용/잔여 예산 + 자동소진 체크박스" 영역을 제거하고,
대신 "예상 참여 크리에이터 인원"을 보여준다. 게임사가 "이 예산이면 몇 명 참여하지?"를 바로 알게.

디자인: 다크 #0A0A0F / 우베 #9B7EC8 / 골드 #E5B567 / Arial Black

**작업 전:**
1. src/app/studio/new/ 의 2단계(StepMissions) 컴포넌트 전체 읽기 — 현재 사용/잔여 예산 계산, 자동소진 체크박스, 미션/등급 선택 상태가 어떻게 되어있는지
2. src/lib/pricing.ts (또는 단가 관련) — RATE_MATRIX, 등급별 단가, estimateCreatorRange 등 기존 계산 함수 확인
3. 미션의 creator_amount(크리에이터 수령 단가)가 등급별로 어떻게 결정되는지 확인
4. /mnt/skills/public/frontend-design/SKILL.md (없으면 skip)

---

## Phase 1: 하단 예산 영역 제거

StepMissions(2단계)에서 아래를 **완전히 제거**:
- "사용 예산 / 잔여 예산" 표시 블록
- "잔여 예산을 하위 등급 미션으로 자동 소진합니다" 체크박스
- 관련 상태(autoSpendRemaining 등)가 다른 곳에서 안 쓰이면 정리. 단, 캠페인 생성 payload에 필요하면 기본값으로 유지(제거로 인해 빌드 깨지지 않게 주의).

총 예산(총 예산 *) 입력 필드는 유지.

---

## Phase 2: 예상 참여 인원 영역 추가

제거한 자리에 "예상 참여 크리에이터" 카드 추가. 보라 그라데이션 배경(from-[#1a1030] to-[#0A0A0F]), 보라 테두리.

### 계산 로직 (헬퍼 함수로 작성)

입력: 총예산, 미션 목록(각 미션의 content_type + 선택된 등급들), 등급별 단가(content_type별)

1. **미션별 예산 배분**: 총예산 ÷ 미션 개수 = 미션별 예산 (균등)
2. 각 미션에서:
   - 그 미션의 content_type에 해당하는 등급별 단가 사용 (creator_amount 기준 = 크리에이터 수령액)
   - **등급별 단독 인원**: floor(미션예산 ÷ 해당 등급 단가)
   - **균등 배분 총 인원**: 미션예산 ÷ 선택등급수 = 등급별 예산 → 각 등급 floor(등급별예산 ÷ 단가) → 합산
3. **전체 집계**:
   - 총 예상 인원(균등) = 모든 미션의 균등배분 인원 합
   - 등급별 단독 인원 = 미션이 1개면 그 값, 여러 개면 미션별 합산(같은 등급끼리)

주의: 등급이 미션마다 다르게 선택될 수 있음. 등급별 단독 표시는 "선택된 등급 전체"에 대해, 각 등급이 등장하는 미션들의 인원을 합산.
0으로 나누기 방지, 선택 등급 없으면 안내 문구.

### 표시 (위→아래)

**상단 — 총 예상 (균등 배분)**
- "예상 참여 크리에이터" 타이틀
- 서브: "선택한 등급에 예산을 고르게 배분할 경우"
- 우측 큰 숫자: "약 N명" (골드, font-black, 균등 배분 총 인원)

**하단 — 등급별 단독 참여 시**
- 구분선 + "등급별로 참여할 경우" 라벨
- 선택된 등급마다 한 줄 (단가는 표시하지 않음):
  - 등급 뱃지(원형, 골드) + "X등급으로만 참여 시" + 우측 "약 N명"
- 등급은 S>A>B>C>D>E 순으로 정렬

예시 (200만, 숏폼 C/D/E):
- 총 예상(균등): 약 13명
- C등급으로만: 약 10명 / D등급으로만: 약 13명 / E등급으로만: 약 20명

### 실시간 반영
- 총예산/미션/등급 선택이 바뀌면 예상 인원도 즉시 갱신 (useMemo 등)

### 디자인
- 단가 금액은 어디에도 노출하지 않음 (인원만)
- 잔여 예산/자동소진 관련 문구 일절 없음
- 모바일 반응형

---

## Phase 3: 검증
1. npx tsc --noEmit → 0 errors
2. npm run build → 성공
3. git add . && git commit -m "feat: estimated participant count in campaign wizard" && git push origin rebuild

최종 보고:
- 제거한 요소 (사용/잔여 예산, 자동소진 체크박스)
- 예상 인원 계산 로직 (균등 배분 총 인원 + 등급별 단독)
- 미션 여러 개일 때 처리
- 단가 비노출 확인
