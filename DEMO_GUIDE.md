# Project Creator — 클라이언트 시연 가이드

전체 플로우를 처음부터 끝까지 보여주기 위한 대본입니다.
시연 플로우: **게임사 가입 → 캠페인 생성 → Admin 승인 → 크리에이터 가입 → 채널 등록 → 캠페인 지원 → 콘텐츠 제출 → Admin 검수 승인 → 크리에이터 수익 확인**

---

## 0. 사전 준비 (시연 30분 전, 1회)

### 0-1. Supabase 준비
1. **새 Supabase 프로젝트** 생성 (권장 — 구 스키마와 충돌 방지).
2. SQL Editor에서 마이그레이션을 **순서대로** 실행:
   - `supabase/migrations/20260611000001_demo_ready.sql` (기본 스키마 + RLS)
     - 기존에 구 `supabase/schema.sql`을 적용한 프로젝트라면, 파일 상단의 "구 스키마 제거"
       블록 주석을 먼저 해제하고 실행. (데모 데이터는 사라짐)
   - `supabase/migrations/20260611000002_credits.sql` (크레딧 충전 + 24시간 홀드)
3. **Authentication → Providers → Email**: "Confirm email"을 **끈다**(데모 중 메일 인증 생략).
4. **Authentication → Hooks**: "Custom Access Token" 훅으로 `public.custom_access_token_hook` 지정.
   - (선택) 역할 클레임이 필요 없으면 생략 가능 — RLS는 `profiles.role` 직접 조회로 동작.
5. 프로젝트 URL / anon key를 `.env.local`에 설정:
   ```
   NEXT_PUBLIC_SUPABASE_URL=...
   NEXT_PUBLIC_SUPABASE_ANON_KEY=...
   ```

### 0-2. 계정 3개 만들기
앱의 `/signup`에서 생성한다.

| 역할 | 이메일(예시) | 가입 방법 |
|------|------|-----------|
| 게임사 | studio@demo.com | `/signup` → "게임사" 선택 |
| 크리에이터 | creator@demo.com | `/signup` → "크리에이터" 선택 |
| 관리자 | admin@demo.com | `/signup`으로 가입(아무 역할) 후 아래 SQL로 승격 |

관리자 승격 (SQL Editor):
```sql
UPDATE public.profiles SET role = 'admin' WHERE email = 'admin@demo.com';
```
승격 후 admin 계정은 **로그아웃 → 재로그인**(토큰 갱신).

### 0-3. 시작
```bash
npm install
npm run dev
```
3개 계정을 각각 다른 브라우저(또는 시크릿 창)에 로그인해두면 전환이 매끄럽다.

---

## 1. 시연 순서 (단계별 스크립트)

### ⓪ 게임사 — 크레딧 충전 (캠페인 생성 전 필수)
- **로그인**: studio@demo.com → `/studio` 대시보드 상단 **크레딧 카드**.
- "충전하기" → 프리셋(100만/300만/500만/1000만) 또는 직접입력 → **테스트 충전**(PG 미연동, 즉시 반영).
- **멘트 포인트**: "게임사는 예산을 **선불 크레딧**으로 충전합니다. 캠페인을 만들면 그만큼 **홀딩**되고, 검수 승인·홀드기간 경과 후 크리에이터에게 지급됩니다."
- 캠페인 총예산 이상으로 충전해두면 다음 단계가 매끄럽다.

### ① 게임사 — 캠페인 생성
- **화면**: `/studio` 대시보드. "캠페인 만들기" 클릭 → `/studio/new`.
- 캠페인 제출 시 총예산이 크레딧에서 **홀딩**된다(available ↓, held ↑). 잔액 부족이면 "크레딧 잔액이 부족합니다" 안내 → 충전 후 재시도.
- **3단계 위저드**:
  1. **게임 정보** — 게임명, 장르, 설명.
  2. **예산·미션 구성** — 총 예산 입력 → 콘텐츠 타입(라이브/롱폼/숏폼)과 참여 가능 등급 선택.
     단가가 등급별로 **자동 계산**되고, 잔여 예산은 하위 등급으로 **자동 배분**된다.
  3. **최종 확인** — 미션별 예상 결과물(약 N명)과 가이드라인 확인 후 제출.
- **멘트 포인트**: "예전 5단계를 **3단계로 간소화**했고, 게임사는 단가를 일일이 계산할 필요가 없습니다. 등급만 고르면 단가가 자동입니다."
- 제출 시 캠페인은 `pending`(승인 대기) 상태가 된다.

### ② 관리자 — 캠페인 승인
- **로그인**: admin@demo.com → `/admin` 대시보드.
- **화면**: 메트릭 카드(전체 캠페인 / **승인 대기** / 검수 대기 / 전체 크리에이터).
- "캠페인 승인" → `/admin/campaigns`. 대기 중 캠페인의 미션 가이드를 검토.
- (선택) Admin 메모 입력 → **승인하기**. 상태가 `active`로 바뀐다.
- **멘트 포인트**: "플랫폼이 캠페인 품질을 **게이트키핑**합니다. 홀드 시 게임사에 수정 요청이 전달됩니다."

### ③ 크리에이터 — 채널 등록
- **로그인**: creator@demo.com → `/creator`.
- 채널 미등록 안내 → "채널 관리" → `/creator/profile`.
- 채널 추가: 플랫폼 / 채널명 / URL / **구독자 수** 입력 → 구독자 수로 **등급 자동 산정**(S~E).
  플랫폼별 지원 콘텐츠 타입만큼 행이 생성된다(예: YouTube = 라이브/롱폼/숏폼).
- **멘트 포인트**: "구독자 수만 넣으면 등급이 자동입니다. 등급이 곧 단가입니다."

### ④ 크리에이터 — 캠페인 지원 (오픈마켓)
- `/creator`로 돌아가면, **내 등급에 맞는 미션이 있는 캠페인만** 노출된다.
- 콘텐츠 타입 필터, 내 수령 예상액(₩) 표시.
- 원하는 콘텐츠 타입에서 **지원하기** → 지원 완료. 해당 미션은 `filled` 처리.
- **멘트 포인트**: "지정 발주가 아니라 **오픈마켓**입니다. 자격이 되는 크리에이터가 직접 지원합니다."

### ⑤ 크리에이터 — 콘텐츠 제출
- `/creator/activity`("내 지원 현황")로 이동.
- 지원 건이 **제작 중(confirmed)** 상태 → **콘텐츠 제출** 버튼.
- 제출 모달: 콘텐츠 타입에 맞는 **플랫폼만** 선택 가능(라이브=YouTube/SOOP/치지직, 숏폼=YouTube/TikTok, 롱폼=YouTube). 멀티 플랫폼은 "+ 플랫폼 추가"로 여러 URL 입력.
- 제출 → 상태가 **검수 중(pending)**으로 바뀐다.

### ⑥ 관리자 — 콘텐츠 검수
- admin → `/admin/payouts`("콘텐츠 검수", 사이드바 라벨도 "콘텐츠 검수").
- 제출된 콘텐츠의 플랫폼 URL과 미션 가이드를 확인.
- **4개 체크리스트**(URL 유효 / 타입 일치 / 길이 충족 / 가이드 충족)를 **모두 체크해야** 승인 활성화.
- **승인** → 상태 `approved`. (거절 시 사유 입력 → 크리에이터에게 노출)
- **멘트 포인트**: "플랫폼이 콘텐츠 품질을 **검수**합니다. 게임사는 검증된 결과물만 받습니다."

### ⑦ 크리에이터 — 수익 확인 (홀드 → 지급)
- creator → `/creator/earnings`("수익 현황"). **진입 시 홀드 끝난 건은 자동 지급 처리**된다(조회 시점).
- 3분류 표시: **지급 완료**(실수령) / **지급 대기**(승인됐고 홀드 진행 중 — "N시간 후 지급") / **검수 대기**.
- 홀드기간이 지난 승인 건은 게임사 held에서 차감되어 크리에이터에게 지급되고 payments에 기록된다.
- **멘트 포인트**: "검수 승인 후 **홀드기간(기본 24시간)**이 지나면 자동 지급됩니다. 게임사 예산은 그 전까지 안전하게 홀딩됩니다."

### ⑧ (선택) 게임사 — 캠페인 완료 & 미집행 예산 복귀
- admin → `/admin/campaigns`에서 진행 중 캠페인 카드의 **"캠페인 완료 처리"** 버튼.
- 미집행분(홀드 합 − 지급 합)이 게임사 **크레딧으로 복귀**(held ↓, available ↑)되고 상태가 `completed`로 바뀐다.
- **멘트 포인트**: "쓰지 않은 예산은 캠페인 종료 시 게임사에게 **자동 환원**됩니다."

> **홀드 시연 팁**: 기본 홀드는 24시간이라 실시간 지급을 보여주기 어렵다. 시연 시에는
> `src/lib/credits.ts`의 `HOLD_DURATION_HOURS` 값을 `0`(즉시) 또는 작은 값으로 바꾸면,
> 검수 승인 직후 크리에이터 수익 페이지 진입만으로 지급이 처리되는 흐름을 보여줄 수 있다.

---

## 2. 시연 중 강조할 4가지 포인트
1. **3단계 위저드** — 캠페인 생성이 간단(게임 정보 → 예산/등급 → 확인).
2. **오픈마켓 지원** — 지정 발주가 아닌, 자격 기반 크리에이터 자발적 지원.
3. **플랫폼 검수** — 4-체크 검수로 콘텐츠 품질 보증.
4. **등급별 자동 단가** — 구독자 수 → 등급 → 단가가 전부 자동.

---

## 3. 빠른 트러블슈팅
- **가입 후 화면이 비어있다** → "Confirm email"이 켜져 있을 가능성. 끄거나 메일 인증 후 로그인.
- **admin 권한이 안 먹는다** → 승격 SQL 실행 후 **재로그인** 했는지 확인.
- **캠페인이 크리에이터에게 안 보인다** → ① 캠페인이 `active`(승인됨)인지 ② 크리에이터 채널 등급이 미션 `allowed_grades`에 포함되는지 확인.
- **자세한 미구현/제약은 `GAPS.md` 참고.**
